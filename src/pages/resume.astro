---
import BaseLayout from "../layouts/BaseLayout.astro";
import resumeData from "../data/resume.json";
---

<BaseLayout
    title="Resume - Shanyu Thibaut"
    description="Professional experience and career timeline"
>
    <div class="max-w-3xl mx-auto">
        <h1 class="text-2xl font-bold mb-7 text-gray-900">Resume</h1>

        <!-- Timeline Container -->
        <div class="relative">
            <!-- Vertical Line -->
            <div class="absolute left-3 top-0 bottom-0 w-0.5 bg-gray-300"></div>

            <!-- Timeline Items -->
            <div class="space-y-7">
                {resumeData.experience.map((item) => (
                    <div class="relative flex items-start">
                        <!-- Timeline Dot -->
                        <div
                            class={`absolute left-1.5 w-3 h-3 ${item.dotColor} rounded-full border-3 border-white shadow-lg z-10`}
                        >
                        </div>
                        
                        <!-- Content -->
                        <div class="ml-10">
                            <div
                                class="flex flex-col md:flex-row md:items-center md:gap-3 mb-2"
                            >
                                <h3 class="text-lg font-bold text-gray-900">
                                    {item.title}
                                </h3>
                                <span
                                    class="text-xs text-gray-500 font-medium bg-gray-100 px-2 py-1 rounded-full"
                                    >{item.period}</span
                                >
                            </div>
                            <div class="flex items-center gap-2 mb-3">
                                {item.logo && (
                                    <img 
                                        src={item.logo} 
                                        alt={`${item.company} logo`}
                                        class="w-6 h-6 rounded-full object-cover border border-gray-200"
                                    />
                                )}
                                <h4 class="text-base text-blue-600 font-semibold">
                                    {item.company}
                                </h4>
                            </div>
                            <p class="text-sm text-gray-700 leading-relaxed">
                                {item.description}
                            </p>
                        </div>
                    </div>
                ))}
            </div>
        </div>

        <!-- Education Section -->
        <div class="mt-14">
            <h2 class="text-xl font-semibold mb-5 text-gray-900">Education</h2>
            <div class="space-y-5">
                {resumeData.education.map((item) => (
                    <div class="bg-white border border-gray-200 rounded-lg p-5">
                        <div
                            class="flex flex-col md:flex-row md:items-center md:justify-between mb-3"
                        >
                            <div class="flex items-center gap-2">
                                {item.logo && (
                                    <img 
                                        src={item.logo} 
                                        alt={`${item.school} logo`}
                                        class="w-8 h-8 rounded-full object-cover border border-gray-200"
                                    />
                                )}
                                <div>
                                    <h3 class="text-lg font-bold text-gray-900">
                                        {item.degree}
                                    </h3>
                                    <h4 class="text-base text-blue-600 font-semibold">
                                        {item.school}
                                    </h4>
                                </div>
                            </div>
                            <span
                                class="text-xs text-gray-500 font-medium bg-gray-100 px-2 py-1 rounded-full mt-2 md:mt-0"
                                >{item.period}</span
                            >
                        </div>
                        <p class="text-sm text-gray-700 leading-relaxed">
                            {item.description}
                        </p>
                    </div>
                ))}
            </div>
        </div>
    </div>

        <!-- Skills Section -->
        <div class="mt-14">
            <h2 class="text-xl font-semibold mb-5 text-gray-900">
                Technical Skills
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-5">
                {Object.entries(resumeData.skills).map(([categoryName, skills]) => (
                    <div class="bg-white border border-gray-200 rounded-lg p-3">
                        <h3 class="text-base font-semibold text-gray-900 mb-2">
                            {categoryName}
                        </h3>
                        <div class="flex flex-wrap gap-1.5">
                            {skills.map((skill) => (
                                <span
                                    class={`${skill.color} text-white px-2 py-1 rounded-full text-xs font-medium`}
                                    >{skill.name}</span
                                >
                            ))}
                        </div>
                    </div>
                ))}
            </div>
        </div>

        
</BaseLayout>
